// Title: Find if Digit Game Can Be Won
            // Difficulty: Easy
            // Language: Python3
            // Link: https://leetcode.com/problems/find-if-digit-game-can-be-won/

class Solution:
    def canAliceWin(self, nums: List[int]) -> bool:
        alice=[]
        bob=[]
        for i in range(len(nums)):
            if nums[i]<10:
                alice.append(nums[i])
            else:
                bob.append(nums[i])
        alice_sum=sum(alice[:len(alice)])
