// Title: Maximum Subarray
            // Difficulty: Medium
            // Language: Python3
            // Link: https://leetcode.com/problems/maximum-subarray/

class Solution:
    def maxSubArray(self, nums: list[int]) -> int:
        ans=nums[0]
        bestsum=nums[0]
        for i in range(1,len(nums)):
            bestsum=max(bestsum+nums[i],nums[i])
            
                    
        return ans   
            ans=max(ans,bestsum)
            
